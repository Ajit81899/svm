# svm
This project demonstrates the working of a Support Vector Machine (SVM) classifier using synthetic data generated with make_blobs. It visualizes how the decision boundary, margins, and support vectors change with different datasets and values of the regularization parameter C. 
